💼 *Portfolio Details*

Total Positions: {{.TotalCount}}

{{range $idx, $pos := .Positions}}
{{add $idx 1}}. *{{$pos.Symbol}}* {{$pos.Side}}
   💰 Entry: ${{printf "%.2f" $pos.EntryPrice}} → Current: ${{printf "%.2f" $pos.CurrentPrice}}
   📊 Amount: {{printf "%.4f" $pos.Amount}}
   {{if ge $pos.UnrealizedPnL 0.0}}💚{{else}}💔{{end}} PnL: ${{printf "%.2f" $pos.UnrealizedPnL}} ({{printf "%.2f" $pos.PnLPercent}}%)
   🛑 Stop: ${{printf "%.2f" $pos.StopLoss}}
   🎯 Target: ${{printf "%.2f" $pos.TakeProfit}}
   ⏱️ Opened: {{$pos.OpenedAt}} ({{$pos.Duration}})

{{end}}
🕐 {{.Timestamp}}

_Use /stop to pause trading_

